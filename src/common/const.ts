export const ITEM_PER_PAGE = 10;

export const ROLES_KEY = 'roles';
export const IS_PUBLIC_KEY = 'isPublic';

export enum Role {
  User = 'user',
  Admin = 'admin',
}

export enum Status {
  INVALID = 0,
  VALID = 1,
}
